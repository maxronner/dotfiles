#!/usr/bin/env bash

[ "$#" -gt 0 ] || { printf "Usage: ? <search terms>\n" >&2; exit 2; }
q="$*"
enc="$(python3 -c 'import sys, urllib.parse; print(urllib.parse.quote_plus(sys.argv[1]))' "$q")" || exit 1
exec w3m "https://duckduckgo.com/html/?q=$enc"
