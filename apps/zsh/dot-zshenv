if [ -f $HOME/.config/shell/base-xdg-paths.sh ]; then
    source "$HOME/.config/shell/base-xdg-paths.sh"
fi

if [ -f $HOME/.config/shell/core-env-exports.sh ]; then
    source "$HOME/.config/shell/core-env-exports.sh"
fi

export ZDOTDIR="${XDG_CONFIG_HOME:-$HOME/.config}/zsh"
