#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
  exit 0
fi

set +e
result=$(calc "$@" 2>&1)
status=$?
set -e

if [[ $status -ne 0 || -z $result ]]; then
  exit 0
fi

echo "$result" | yankcmd
tmux display-message -- "$result"
